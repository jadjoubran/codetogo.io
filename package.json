{
  "private": true,
  "devDependencies": {
    "algoliasearch": "^3.24.5",
    "gulp": "^3.8.8",
    "hasbin": "^1.2.3",
    "laravel-elixir": "^6.0.0-15",
    "laravel-elixir-browsersync-official": "^1.0.0",
    "yargs": "^4.6.0"
  },
  "scripts": {
    "sync": "node tasks/algolia-sync.js",
    "deploy": "npm run build && git add dist -f && git commit -m 'Build for production' && git subtree push --prefix dist www master"
  }
}
